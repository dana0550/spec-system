interface:
  display_name: "Docs Spec System v2"
  short_description: "Phase-gated spec system with strict traceability"
  default_prompt: "Use $docs-spec-system and specctl to run the v2 phase-gated workflow (requirements -> design -> tasks -> verification) with EARS+RFC requirements, Gherkin scenarios, and full traceability."
