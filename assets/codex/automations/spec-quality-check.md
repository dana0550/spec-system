# Spec Quality Check Automation Prompt

Run `specctl check --root .` and `specctl report --root . --json`.
Summarize errors and warnings by code and path.
If there are no errors, summarize drift risks and open quality gaps.
